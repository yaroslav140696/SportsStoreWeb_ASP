@model SportsStore.Domain.Entities.CurrentUser

<ul class="nav navbar-nav navbar-right">
    @if (Model.Data.Email == "null@null")
    {
        <li>
            <a href="@Url.Action("Login", "Account", new { returnUrl = Request.Url.PathAndQuery })">Войти <span class = "glyphicon glyphicon-log-in"></span></a>
        </li>
    }
    else
    {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <span class="glyphicon glyphicon-user"></span>
                @Model.Data.FirtsName
                <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
                @if (Model.isAdmin)
                {
                    <li>
                        @Html.ActionLink("Администрирование", "Tables", "Admin")
                    </li>
                    <li class="divider"></li>
                }
                <li>@Html.RouteLink("Редактировать профиль", 
                   new { controller = "AdminUser", action = "Edit", itemID = Model.Data.userID })
                </li>
                <li>@Html.ActionLink("Мои заказы", "qwer", "qwer")</li>
                <li>@Html.ActionLink("Мои желания", "qwer", "qwer")</li>
            </ul>
        </li>
        <li>
            <a href="@Url.Action("Logout", "Account", new { returnUrl = Request.Url.PathAndQuery })">Выйти <span class="glyphicon glyphicon-log-out"></span></a>
        </li>
    }
</ul>
