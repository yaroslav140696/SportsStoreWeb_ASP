@model SportsStore.Domain.Entities.Cart

<ul class="nav navbar-nav navbar-right">
    <li><a href="#"><span class="glyphicon glyphicon-shopping-cart"></span>
         @Model.Lines.Sum(x => x.Quantity) товар(ов)
    @Model.ComputeTotalValue().ToString("c")</a></li>
    <li><a href="#"><span class="glyphicon glyphicon-log-in"></span>
    
        </a></li>
</ul>
    @*<ul class="navbar navbar-right">
        <li class="navbar-btn" style="display:inline">
            @Html.ActionLink("Корзина", "Index", "Cart",
    new
    {
        returnUrl = Request.Url.PathAndQuery
    },
    new
    {
        @class = "btn btn-primary glyphicon glyphicon-shopping-cart"
    })
        </li>
        <li class="navbar-text">
            @Model.Lines.Sum(x => x.Quantity) товар(ов)
            @Model.ComputeTotalValue().ToString("c")
        </li>
    </ul>*@

